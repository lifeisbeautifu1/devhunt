// import { useLocation } from 'react-router-dom';
import { Header, Featured, Posts } from '../components';

const Home = () => {
  return (
    <>
      <Header />
      <Featured />
      <Posts />
    </>
  );
};

export default Home;
